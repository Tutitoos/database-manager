import { ComingSoon } from "@/components/coming-soon";

/** Stubbed during UI rebuild — restore from `main` when ready to migrate. */
export default function ConnectionsPage() {
  return <ComingSoon title="Conexiones" subtitle="Esta vista se está rediseñando." />;
}
