import { ComingSoon } from "@/components/coming-soon";
import type { DocumentSession } from "@/store/sessions";

/** Stubbed during UI rebuild — restore from `main` when ready to migrate. */
export default function DocumentLayout() {
  return <ComingSoon title="MongoDB" subtitle="Esta vista se está rediseñando." />;
}

export type { DocumentSession };
