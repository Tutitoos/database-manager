import { ComingSoon } from "@/components/coming-soon";
import type { SqlSession } from "@/store/sessions";

/** Stubbed during UI rebuild — restore from `main` when ready to migrate. */
export default function SqlLayout() {
  return <ComingSoon title="SQL" subtitle="Esta vista se está rediseñando." />;
}

export type { SqlSession };
