import { ComingSoon } from "@/components/coming-soon";

/** Stubbed during UI rebuild — restore from `main` when ready to migrate. */
export default function DashboardPage() {
  return <ComingSoon title="Dashboard" subtitle="Esta vista se está rediseñando." />;
}
